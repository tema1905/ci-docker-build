# CI Docker Build

Файл AllFrames.py собирается в Docker-образ при помощи Jenkins.

## Содержимое:
- AllFrames.py — исходный код
- Dockerfile — инструкция сборки Docker-образа
- build.sh — скрипт сборки
- Jenkinsfile — CI pipeline
